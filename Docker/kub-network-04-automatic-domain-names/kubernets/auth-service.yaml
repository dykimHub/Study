apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  selector:
    app: auth
  # auth-api는 user-api 뿐만 아니라 tasks-api와도 통신해야하기 때문에 분리했지만
  # 여전히 외부로 노출할 필요는 없어서 클러스터 내에서만 접근 가능한 ClusterIP type을 사용
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
