apiVersion: v1
kind: Service
metadata:
  name: tasks-service
spec:
  selector:
    app: tasks
  type: LoadBalancer # 외부에 노출되어야 하는 app
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
